@using Domain.Entities;
@model WA_HamburgerProjesiMVC_100124.Models.MenuDetailsVM

@{
    ViewData["Title"] = "MenuDetails";
    Layout = "AdminLayout_";
}

<h1>Menu</h1>

<div>
    <h4>Details</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Description)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Description)
        </dd>
        <dt class="col-sm-2">
            Image
        </dt>
        <dd class="col-sm-10">
            <img src="/@Model.ImagePath" alt="Menu Image" class="img-thumbnail" />
        </dd>
    </dl>
</div>



<h3>Menu Products</h3>

@if (Model.Products.Count() == 0)
{
    <tr colspan="2"><p class="text-warning"> There are no products inside the menu.</p></tr>
}

<div class="d-flex flex-wrap">
    @foreach (Product item in Model.Products)
    {
        @*  <div class="container mt-5">
    <div class="card">
    <img src="/@item.ImagePath" class="card-img-top" alt="Card Image">
    <div class="card-body">
    <h5 class="card-title">@item.Name</h5>
    <p class="card-text"></p>

    </div>
    </div>
    </div> *@

        <div class="card" style="width: 15rem; height: 11rem;">
            <img class="card-img" src="/@item.ImagePath" alt="Card image cap">
            <div class="card-title">
                <p class="card-text text-center">@item.Name</p>
            </div>
        </div>
    }
</div>

<div>
    @Html.ActionLink("Edit", "UpdateMenu", new { id = Model.Id }) |
    <a asp-action="Menus">Back to List</a>
</div>
